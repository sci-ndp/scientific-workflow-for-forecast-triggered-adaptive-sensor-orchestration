# Streams MCP Module

Exposes tools for creating Kafka producers in SciDX.

## Capabilities
- Start Kafka streams from consumption methods (`create_kafka_stream`).

## Loading Only This Module
```python
streaming.configure_mcp(model="beeai-local", modules="streams")
```
